{
  "name": "ether-lotto",
  "description": "",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd ./webapp && npm install && cd ../testrpc && npm install && cd ../",
    "test": "truffle test",
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "testrpc": "rimraf ./build/contracts/*.json && cd testrpc && npm start",
    "migrate:kovan": "truffle migrate --network kovan"
  },
  "devDependencies": {
    "bignumber.js": "^4.0.4",
    "ramda": "^0.24.1",
    "@digix/tempo": "^0.2.0"
  },
  "dependencies": {
    "rimraf": "^2.6.2",
    "truffle": "^3.4.9",
    "web3": "^0.20.1"
  }
}
